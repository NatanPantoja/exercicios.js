/*3. Removendo o último elemento
Crie um array com os elementos [1, 2, 3, 4, 5].
Remova o último número usando o método pop() e exiba o array atualizado no console. */

const lista = [1, 2, 3, 4, 5];

lista.pop();

console.log('Lista atualizada', lista);